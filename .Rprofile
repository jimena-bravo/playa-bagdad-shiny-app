source("renv/activate.R")

# --------------------------------------------------------------------------
# Configuración del Repositorio de Paquetes
# --------------------------------------------------------------------------
# Este archivo asegura que tanto tu entorno local como el servidor de 
# shinyapps.io usen el Posit Package Manager. Esto ayuda a evitar 
# errores de compilación al descargar binarios pre-compilados de los 
# paquetes en lugar de construirlos desde la fuente.
# --------------------------------------------------------------------------

options(repos = c(CRAN = "https://packagemanager.posit.co/cran/latest"))
